namespace: app\backend\tests
actor_suffix: Tester
paths:
    tests: app/backend/tests
    output: app/backend/tests/_output
    data: app/backend/tests/_data
    support: app/backend/tests/_support
bootstrap: _bootstrap.php
settings:
    colors: true
    memory_limit: 1024M
modules:
    config:
        Yii2:
            configFile: 'config/codeception-local.php'

# @cli ./vendor/bin/codecept run app/backend/tests